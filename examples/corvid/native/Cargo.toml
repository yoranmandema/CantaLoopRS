[package]
name = "melonjuice-native"
version = "0.1.0"
edition = "2021"

[lib]
name = "native_modules"
path = "lib.rs"
crate-type = ["cdylib"]

[dependencies]
cantaloop = { path = "../../.." }  # Using local path since we're in the cantaloop repo
lazy_static = "1.5.0"
bevy = { version ="0.17.3" }
