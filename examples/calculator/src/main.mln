use print from std;
use str_len from string;
use is_ws, is_digit from utils;

let expr = "1 + 2";
let is_parsing_num = false;
let points = 0;

for i in 0..str_len(expr)! {
    let c = expr[i];
    let ws = is_ws(c)!;

    if ws {
        continue;
    }

    let is_point = c == ".";
    let digit = is_digit(c)! || is_point;

    if !is_parsing_num {
        if digit || is_point {
            is_parsing_num = true;
        }
    }

    if is_parsing_num {
        
    }
}