{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Extension (Development)",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}/.cantaloop-language"
            ],
            "outFiles": [
                "${workspaceFolder}/.cantaloop-language/**/*.js"
            ]
        }
    ]
}
